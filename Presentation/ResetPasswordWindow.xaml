<Window x:Class="Shop.Presentation.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="استعادة كلمة المرور" Height="220" Width="420" FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="أدخل اسم المستخدم والبريد الإلكتروني للحصول على رمز الاستعادة" TextWrapping="Wrap" />

        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock Text="اسم المستخدم" />
            <TextBox x:Name="UsernameBox" Height="30" />
            <TextBlock Text="البريد الإلكتروني" Margin="0,8,0,0" />
            <TextBox x:Name="EmailBox" Height="30" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="SendBtn" Width="140" Click="SendBtn_Click">إرسال رمز عبر الإيميل</Button>
            <Button Width="120" Margin="8,0,0,0" Click="CancelBtn_Click">إلغاء</Button>
        </StackPanel>

    </Grid>
</Window>
