<Window x:Class="Shop.Presentation.ResetPasswordConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تأكيد استعادة كلمة المرور" Height="220" Width="420" FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="أدخل الرمز الذي استلمته وكلمة سر جديدة" TextWrapping="Wrap" />

        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock Text="اسم المستخدم" />
            <TextBox x:Name="UsernameBox" Height="30" IsReadOnly="True" />
            <TextBlock Text="الرمز" Margin="0,8,0,0" />
            <TextBox x:Name="TokenBox" Height="30" />
            <TextBlock Text="كلمة السر الجديدة" Margin="0,8,0,0" />
            <PasswordBox x:Name="NewPasswordBox" Height="30" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="ConfirmBtn" Width="140" Click="ConfirmBtn_Click">تأكيد وتغيير كلمة السر</Button>
            <Button Width="120" Margin="8,0,0,0" Click="CancelBtn_Click">إلغاء</Button>
        </StackPanel>

    </Grid>
</Window>
